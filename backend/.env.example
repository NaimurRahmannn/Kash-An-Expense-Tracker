# Storage driver: csv for local assignment mode, postgres for production
STORAGE_DRIVER=csv

# CSV local files
CSV_USER_FILE=data/users.csv
CSV_EXPENSE_FILE=data/expenses.csv

# Postgres production config
POSTGRES_DSN=
POSTGRES_AUTO_MIGRATE=false
